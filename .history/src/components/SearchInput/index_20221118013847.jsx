import React from 'react'

const SearchInput = ({} ) => {
  return (
    <input
          value={searchValue}
          onChange={this.handleChange} 
          type="search" 
        />
  )
}

export default SearchInput